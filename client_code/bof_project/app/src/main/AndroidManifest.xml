<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bof_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bof_project"
        android:usesCleartextTraffic="true">
        <activity
            android:name="activities.TestActivity"
            android:exported="false" />
        <activity
            android:name="activities.ManageUserActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name="fragments.DatePickerFragment" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name="activities.AlterFileActivity"
            android:exported="false" />
        <activity
            android:name="activities.ShowLikeActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name="adapter.SearchAdapter" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name="activities.IncludeInfoActivity"
            android:exported="false" />
        <activity
            android:name="activities.GameInfoActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name="adapter.GameAdapter" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name="activities.PlayBofActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name="adapter.MusicListAdapter" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name="activities.MusicListActivity"
            android:exported="false" />
        <activity
            android:name="activities.BofInfoActivity"
            android:exported="false" /> <!-- <activity -->
        <!-- android:name="merge.ContestMerge" -->
        <!-- android:exported="false" /> -->
        <!-- <activity -->
        <!-- android:name="SQLite.DBOpenHelper" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name="activities.MainpageActivity"
            android:exported="false" />
        <activity
            android:name="activities.RegisterActivity"
            android:exported="false" />
        <activity android:name="activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>